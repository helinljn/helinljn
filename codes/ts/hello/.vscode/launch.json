{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Run tutorial navigator",
      "program": "${workspaceFolder}/dist/main.js",
      "preLaunchTask": "npm: build",
      "cwd": "${workspaceFolder}"
    }
  ]
}
