# 项目 1：文件批处理工具

这个项目演示 TypeScript 在 Node.js CLI 场景下的基本工程组织：参数解析、目录遍历、文本转换、错误传播和可测试核心函数。

```bat
npm run build
node dist/11_项目实战/chapter12_项目1_文件批处理工具/main.js --input input --output output --mode upper
```

支持模式：

- `upper`：转大写
- `lower`：转小写
- `prefix-lines`：给每一行加前缀和行号
